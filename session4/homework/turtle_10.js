// square
const postion = {
        shape: 'square',
        x: 20,
        y: 40,
        width: 50
}
rt(90)
fd(postion.x)
lt(90)
fd(postion.y)
for (let i = 1; i <= 4; i++) {
        fd(postion.width)
        rt(90)
}

// rect
home()
const postion = {
        shape: 'rect',
        x: 8,
        y: 70,
        width: 12,
        height: 40
}
rt(90)
penup()
fd(postion.x)
lt(90)
fd(postion.y)

for (let i = 0; i < 2; i++) {
        fd(postion.height)
        rt(90)
        fd(postion.width)
        rt(90)
}

// rect
home()
const postion = {
        shape: 'rect',
        x: 70,
        y: 70,
        width: 12,
        height: 40
}
rt(90)
penup()
fd(postion.x)
lt(90)
fd(postion.y)

for (let i = 0; i < 2; i++) {
        fd(postion.height)
        rt(90)
        fd(postion.width)
        rt(90)
}


// rect
home()
const postion = {
        shape: 'rect',
        x: 10,
        y: 20,
        width: 70,
        height: 20
}
rt(90)
penup()
fd(postion.x)
lt(90)
fd(postion.y)

for (let i = 0; i < 2; i++) {
        fd(postion.height)
        rt(90)
        fd(postion.width)
        rt(90)
}

// square
home()
const postion = {
        shape: 'rect',
        x: 25,
        y: 40,
        width: 14,
}
rt(90)
penup()
fd(postion.x)
lt(90)
fd(postion.y)

for (let i = 0; i < 4; i++) {
        fd(postion.width)
}

// square
home()
const postion = {
        shape: 'rect',
        x: 33,
        y: 42,
        width: 6,
}
rt(90)
penup()
fd(postion.x)
lt(90)
fd(postion.y)

for (let i = 0; i < 4; i++) {
        fd(postion.width)
}

// square
home()
const postion = {
        shape: 'rect',
        x: 50,
        y: 40,
        width: 14,
}
rt(90)
penup()
fd(postion.x)
lt(90)
fd(postion.y)

for (let i = 0; i < 4; i++) {
        fd(postion.width)
}

// square
home()
const postion = {
        shape: 'rect',
        x: 58,
        y: 40,
        width: 14,
}
rt(90)
penup()
fd(postion.x)
lt(90)
fd(postion.y)

for (let i = 0; i < 4; i++) {
        fd(postion.width)
}

// square
home()
const postion = {
        shape: 'rect',
        x: 58,
        y: 42,
        width: 6,
}
rt(90)
penup()
fd(postion.x)
lt(90)
fd(postion.y)

for (let i = 0; i < 4; i++) {
        fd(postion.width)
}

// square
home()
const postion = {
        shape: 'rect',
        x: 40,
        y: 25,
        width: 8,
}
rt(90)
penup()
fd(postion.x)
lt(90)
fd(postion.y)

for (let i = 0; i < 4; i++) {
        fd(postion.width)
}

// square
home()
const postion = {
        shape: 'rect',
        x: 500,
        y: 500,
        width: 15,
}
rt(90)
penup()
fd(postion.x)
lt(90)
fd(postion.y)

for (let i = 0; i < 4; i++) {
        fd(postion.width)
}


